MONGO_URI=mongodb://mongo:27017/florencedb
JWT_SECRET=change_me_to_a_long_random_secret
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1
S3_BUCKET_NAME=
PORT=3000
KAFKA_BROKER=kafka:9092
# Comma-separated list of additional IPs allowed to call /internal/* endpoints
# Localhost (127.0.0.1 / ::1) is always allowed
INTERNAL_ALLOWED_IPS=
# AWS SES — sender address must be a verified SES identity
SES_FROM_EMAIL=
# Optional: override region for SES if different from S3 bucket region
SES_REGION=
